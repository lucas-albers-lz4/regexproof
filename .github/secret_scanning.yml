# Narrow exclusions for intentional detector fixture corpora.
# Do NOT ignore all of properties/generated/ — SAT witnesses must still scan.
# See Phase 5 plan Step 0 / docs: gitleaks keywords are upstream test strings.
paths-ignore:
  - "pilots/gitleaks/config/**"
  - "properties/generated/gitleaks_extractor.jsonl"
  - "properties/generated/gitleaks_encodable_fraction.json"
