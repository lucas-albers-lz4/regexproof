# hermes-agent (Phase 3 delta) — sample freeze only

Full tree is not vendored. Frozen sample `sites.jsonl` anchors ID stability
for before/after reporting. See `properties/generated/hermes_agent_delta.json`.
