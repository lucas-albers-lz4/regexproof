{
  "schema_version": "1",
  "corpus_type": "rule_corpus",
  "questions": [
    {
      "id": "rc-shape1-injection-alphabet",
      "shape": 1,
      "threat": "Rule language admits control/injection characters unexpected for a secret token",
      "encoding_hint": "alphabet disjointness over single-char membership",
      "applies_when": "encodable token-like patterns"
    },
    {
      "id": "rc-shape2-missing-keyword",
      "shape": 2,
      "threat": "Regex accepts a string lacking its required keyword/prefix",
      "encoding_hint": "whitelist membership + Contains negation for required prefix",
      "applies_when": "rules with fixed literal prefixes"
    },
    {
      "id": "rc-shape3-capture-truncation",
      "shape": 3,
      "threat": "Fallback capture truncates or mismatches true token value",
      "encoding_hint": "IndexOf/SubString counterexample finder",
      "applies_when": "rules with capture groups used as fallbacks"
    },
    {
      "id": "rc-shape4-escape-image",
      "shape": 4,
      "threat": "If rule output is escaped into logs/shell, raw controls must not appear",
      "encoding_hint": "per-token image alphabet check",
      "applies_when": "rare for pure detectors; reserved for escaper-adjacent rules"
    }
  ]
}
