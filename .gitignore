.venv/
.venv
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
*.egg-info/
.eggs/
dist/
build/
.cache/
helpers/ecma/node_modules/
helpers/redos/node_modules/
helpers/go-re2/go-re2
helpers/go-re2/go-re2.exe
redos-env/
batch/corpora/coreruleset/rules

# Unredacted SAT witnesses (search-semantics replay; never commit)
properties/generated/.search-replay-private.json

# Admission probe drafts (authoring inputs; promote via gate_decision only)
properties/generated/*_probe_draft.json

# Unreviewed batch-probe drafts (disclosure hygiene). Walker CI remains
# the real defense against treating these as contract material (#580).
properties/staged_probes/

# Ephemeral batch-probe clone caches / admission leases (never commit)
batch/cache-*/
batch/admission.json
batch/admission.json.lock
batch/*.json.lock
batch/*.json.bak

# Materialized corpus checkouts (see batch/corpora/*/README.md)
batch/corpora/*/rules
batch/corpora/*/detectors
batch/corpora/*/plugins
batch/corpora/*/testdata
batch/corpora/*/testsuite
batch/corpora/*/repo
batch/corpora/test262/RegExp
batch/corpora/rust_regex/src

# Working wave clones (sibling-checkout model)
wave/
cache/
